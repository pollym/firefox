[hover-state-caused-by-compatibility-mouse-events.tentative.html]
  [The compatibility mouse events should set :hover, but shouldn't be preserved after the target is replaced]
    expected:
      if (os == "linux") and not debug: [PASS, FAIL]

  [The compatibility mouse events should set :hover, but shouldn't be preserved after the target is replaced: mouseleave: The tapped element should  have :hover state]
    expected: FAIL
