[browser.storage.extension.html]
  expected:
    if (os == "mac") and not debug and (processor == "x86_64"): OK
    if (os == "mac") and not debug and (processor == "aarch64"): [TIMEOUT, OK]
    if (os == "linux") and asan: [TIMEOUT, OK]
    if os == "android": ERROR
    [OK, TIMEOUT]
  [testStorageOnChanged]
    expected:
      if (os == "mac") and not debug and (processor == "aarch64"): NOTRUN
      if (os == "mac") and debug: PASS
      if (os == "linux") and asan: [NOTRUN, PASS]
      if os == "win": [PASS, FAIL, NOTRUN]
      [PASS, NOTRUN]

  [testStorageSetAccessLevelTrustedContexts]
    expected: FAIL

  [testStorageSetAccessLevelTrustedAndUntrustedContexts]
    expected: FAIL
