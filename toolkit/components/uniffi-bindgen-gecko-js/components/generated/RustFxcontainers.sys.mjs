// This file was autogenerated by the `uniffi-bindgen-gecko-js` crate.
// Trust me, you don't want to mess with it!

import {
  ArrayBufferDataStream,
  FfiConverter,
  FfiConverterArrayBuffer,
  FfiConverterInt8,
  FfiConverterUInt8,
  FfiConverterInt16,
  FfiConverterUInt16,
  FfiConverterInt32,
  FfiConverterUInt32,
  FfiConverterInt64,
  FfiConverterUInt64,
  FfiConverterFloat32,
  FfiConverterFloat64,
  FfiConverterBoolean,
  FfiConverterBytes,
  FfiConverterString,
  UniFFICallbackHandler,
  UniFFICallbackMethodHandler,
  UniFFIError,
  UniFFIInternalError,
  UniFFITypeError,
  constructUniffiObject,
  handleRustResult,
  uniffiObjectPtr,
} from "moz-src:///toolkit/components/uniffi-js/js/UniFFI.sys.mjs";

// Objects intended to be used in the unit tests
export var UnitTestObjs = {
    uniffiObjectPtr,
};
/**
 * colorCode
 * @param {ContainerColor[keyof ContainerColor]} color
 * @param {boolean} nova
 * @returns {string}
 */
export function colorCode(
    color, 
    nova) {
   
FfiConverterTypeContainerColor.checkType(color);
FfiConverterBoolean.checkType(nova);
const result = UniFFIScaffolding.callSync(
    6, // uniffi_fxcontainers_fn_func_color_code
    FfiConverterTypeContainerColor.lower(color),
    FfiConverterBoolean.lower(nova),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}

/**
 * colorFromName
 * @param {string} name
 * @returns {?ContainerColor[keyof ContainerColor]}
 */
export function colorFromName(
    name) {
   
FfiConverterString.checkType(name);
const result = UniFFIScaffolding.callSync(
    7, // uniffi_fxcontainers_fn_func_color_from_name
    FfiConverterString.lower(name),
)
return handleRustResult(
    result,
    FfiConverterOptionalTypeContainerColor.lift.bind(FfiConverterOptionalTypeContainerColor),
    null,
)
}

/**
 * colorGeckoL10nId
 * @param {ContainerColor[keyof ContainerColor]} color
 * @returns {string}
 */
export function colorGeckoL10nId(
    color) {
   
FfiConverterTypeContainerColor.checkType(color);
const result = UniFFIScaffolding.callSync(
    8, // uniffi_fxcontainers_fn_func_color_gecko_l10n_id
    FfiConverterTypeContainerColor.lower(color),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}

/**
 * colorName
 * @param {ContainerColor[keyof ContainerColor]} color
 * @returns {string}
 */
export function colorName(
    color) {
   
FfiConverterTypeContainerColor.checkType(color);
const result = UniFFIScaffolding.callSync(
    9, // uniffi_fxcontainers_fn_func_color_name
    FfiConverterTypeContainerColor.lower(color),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}

/**
 * containerColorAliases
 * @returns {Map}
 */
export function containerColorAliases() {
   
const result = UniFFIScaffolding.callSync(
    10, // uniffi_fxcontainers_fn_func_container_color_aliases
)
return handleRustResult(
    result,
    FfiConverterMapStringString.lift.bind(FfiConverterMapStringString),
    null,
)
}

/**
 * containerColors
 * @returns {Array.<ContainerColor[keyof ContainerColor]>}
 */
export function containerColors() {
   
const result = UniFFIScaffolding.callSync(
    11, // uniffi_fxcontainers_fn_func_container_colors
)
return handleRustResult(
    result,
    FfiConverterSequenceTypeContainerColor.lift.bind(FfiConverterSequenceTypeContainerColor),
    null,
)
}

/**
 * containerIcons
 * @returns {Array.<ContainerIcon[keyof ContainerIcon]>}
 */
export function containerIcons() {
   
const result = UniFFIScaffolding.callSync(
    12, // uniffi_fxcontainers_fn_func_container_icons
)
return handleRustResult(
    result,
    FfiConverterSequenceTypeContainerIcon.lift.bind(FfiConverterSequenceTypeContainerIcon),
    null,
)
}

/**
 * iconFromName
 * @param {string} name
 * @returns {?ContainerIcon[keyof ContainerIcon]}
 */
export function iconFromName(
    name) {
   
FfiConverterString.checkType(name);
const result = UniFFIScaffolding.callSync(
    13, // uniffi_fxcontainers_fn_func_icon_from_name
    FfiConverterString.lower(name),
)
return handleRustResult(
    result,
    FfiConverterOptionalTypeContainerIcon.lift.bind(FfiConverterOptionalTypeContainerIcon),
    null,
)
}

/**
 * iconGeckoL10nId
 * @param {ContainerIcon[keyof ContainerIcon]} icon
 * @returns {string}
 */
export function iconGeckoL10nId(
    icon) {
   
FfiConverterTypeContainerIcon.checkType(icon);
const result = UniFFIScaffolding.callSync(
    14, // uniffi_fxcontainers_fn_func_icon_gecko_l10n_id
    FfiConverterTypeContainerIcon.lower(icon),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}

/**
 * iconName
 * @param {ContainerIcon[keyof ContainerIcon]} icon
 * @returns {string}
 */
export function iconName(
    icon) {
   
FfiConverterTypeContainerIcon.checkType(icon);
const result = UniFFIScaffolding.callSync(
    15, // uniffi_fxcontainers_fn_func_icon_name
    FfiConverterTypeContainerIcon.lower(icon),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}

/**
 * labelGeckoL10nId
 * @param {ContainerLabel[keyof ContainerLabel]} label
 * @returns {?string}
 */
export function labelGeckoL10nId(
    label) {
   
FfiConverterTypeContainerLabel.checkType(label);
const result = UniFFIScaffolding.callSync(
    16, // uniffi_fxcontainers_fn_func_label_gecko_l10n_id
    FfiConverterTypeContainerLabel.lower(label),
)
return handleRustResult(
    result,
    FfiConverterOptionalString.lift.bind(FfiConverterOptionalString),
    null,
)
}

/**
 * latestVersion
 * @returns {number}
 */
export function latestVersion() {
   
const result = UniFFIScaffolding.callSync(
    17, // uniffi_fxcontainers_fn_func_latest_version
)
return handleRustResult(
    result,
    FfiConverterUInt32.lift.bind(FfiConverterUInt32),
    null,
)
}

/**
 * maxUserContextId
 * @returns {number}
 */
export function maxUserContextId() {
   
const result = UniFFIScaffolding.callSync(
    18, // uniffi_fxcontainers_fn_func_max_user_context_id
)
return handleRustResult(
    result,
    FfiConverterUInt32.lift.bind(FfiConverterUInt32),
    null,
)
}

/**
 * Lower cased, IDN-encoded host, or `None` when `site` cannot be bound to a
 * container.
 * @param {string} site
 * @returns {?string}
 */
export function normalizeSite(
    site) {
   
FfiConverterString.checkType(site);
const result = UniFFIScaffolding.callSync(
    19, // uniffi_fxcontainers_fn_func_normalize_site
    FfiConverterString.lower(site),
)
return handleRustResult(
    result,
    FfiConverterOptionalString.lift.bind(FfiConverterOptionalString),
    null,
)
}

/**
 * resolveColor
 * @param {string} name
 * @returns {string}
 */
export function resolveColor(
    name) {
   
FfiConverterString.checkType(name);
const result = UniFFIScaffolding.callSync(
    20, // uniffi_fxcontainers_fn_func_resolve_color
    FfiConverterString.lower(name),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    null,
)
}








/**
 * The icons a container can carry. The set is fixed, so a container cannot end
 * up with an icon the embedder has no artwork for.
 * 
 * A stored document holds the name, not the variant: one written by a newer
 * Firefox can carry an icon this crate does not know, which reads back as
 * `None` and is left in the document untouched.
 */
export const ContainerIcon = Object.freeze({
    /**
     * FINGERPRINT
     */
    FINGERPRINT: 0,
    /**
     * BRIEFCASE
     */
    BRIEFCASE: 1,
    /**
     * DOLLAR
     */
    DOLLAR: 2,
    /**
     * CART
     */
    CART: 3,
    /**
     * VACATION
     */
    VACATION: 4,
    /**
     * GIFT
     */
    GIFT: 5,
    /**
     * FOOD
     */
    FOOD: 6,
    /**
     * FRUIT
     */
    FRUIT: 7,
    /**
     * PET
     */
    PET: 8,
    /**
     * TREE
     */
    TREE: 9,
    /**
     * CHILL
     */
    CHILL: 10,
    /**
     * CIRCLE
     */
    CIRCLE: 11,
    /**
     * FENCE
     */
    FENCE: 12,
});

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainerIcon extends FfiConverterArrayBuffer {
    static #validValues = Object.values(ContainerIcon)

    static read(dataStream) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        switch (dataStream.readInt32()) {
            case 1:
                return ContainerIcon.FINGERPRINT
            case 2:
                return ContainerIcon.BRIEFCASE
            case 3:
                return ContainerIcon.DOLLAR
            case 4:
                return ContainerIcon.CART
            case 5:
                return ContainerIcon.VACATION
            case 6:
                return ContainerIcon.GIFT
            case 7:
                return ContainerIcon.FOOD
            case 8:
                return ContainerIcon.FRUIT
            case 9:
                return ContainerIcon.PET
            case 10:
                return ContainerIcon.TREE
            case 11:
                return ContainerIcon.CHILL
            case 12:
                return ContainerIcon.CIRCLE
            case 13:
                return ContainerIcon.FENCE
            default:
                throw new UniFFITypeError("Unknown ContainerIcon variant");
        }
    }

    static write(dataStream, value) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        if (value === ContainerIcon.FINGERPRINT) {
            dataStream.writeInt32(1);
            return;
        }
        if (value === ContainerIcon.BRIEFCASE) {
            dataStream.writeInt32(2);
            return;
        }
        if (value === ContainerIcon.DOLLAR) {
            dataStream.writeInt32(3);
            return;
        }
        if (value === ContainerIcon.CART) {
            dataStream.writeInt32(4);
            return;
        }
        if (value === ContainerIcon.VACATION) {
            dataStream.writeInt32(5);
            return;
        }
        if (value === ContainerIcon.GIFT) {
            dataStream.writeInt32(6);
            return;
        }
        if (value === ContainerIcon.FOOD) {
            dataStream.writeInt32(7);
            return;
        }
        if (value === ContainerIcon.FRUIT) {
            dataStream.writeInt32(8);
            return;
        }
        if (value === ContainerIcon.PET) {
            dataStream.writeInt32(9);
            return;
        }
        if (value === ContainerIcon.TREE) {
            dataStream.writeInt32(10);
            return;
        }
        if (value === ContainerIcon.CHILL) {
            dataStream.writeInt32(11);
            return;
        }
        if (value === ContainerIcon.CIRCLE) {
            dataStream.writeInt32(12);
            return;
        }
        if (value === ContainerIcon.FENCE) {
            dataStream.writeInt32(13);
            return;
        }
        throw new UniFFITypeError("Unknown ContainerIcon variant");
    }

    static computeSize(value) {
        return 4;
    }

    static checkType(value) {
      // Check that the value is a valid enum variant
      if (!this.#validValues.includes(value)) {
          throw new UniFFITypeError(`${value} is not a valid value for ContainerIcon`);
      }
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalTypeContainerIcon extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterTypeContainerIcon.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterTypeContainerIcon.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterTypeContainerIcon.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterTypeContainerIcon.computeSize(value)
    }
}

/**
 * The colors a container can carry.
 * 
 * A stored document holds the name, not the variant, and the same caveats as
 * [`ContainerIcon`] apply. Legacy names are not variants: they are resolved to
 * their canonical replacement by [`resolve_color`].
 */
export const ContainerColor = Object.freeze({
    /**
     * GRAY
     */
    GRAY: 0,
    /**
     * YELLOW
     */
    YELLOW: 1,
    /**
     * ORANGE
     */
    ORANGE: 2,
    /**
     * RED
     */
    RED: 3,
    /**
     * PINK
     */
    PINK: 4,
    /**
     * PURPLE
     */
    PURPLE: 5,
    /**
     * VIOLET
     */
    VIOLET: 6,
    /**
     * BLUE
     */
    BLUE: 7,
    /**
     * CYAN
     */
    CYAN: 8,
    /**
     * GREEN
     */
    GREEN: 9,
});

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainerColor extends FfiConverterArrayBuffer {
    static #validValues = Object.values(ContainerColor)

    static read(dataStream) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        switch (dataStream.readInt32()) {
            case 1:
                return ContainerColor.GRAY
            case 2:
                return ContainerColor.YELLOW
            case 3:
                return ContainerColor.ORANGE
            case 4:
                return ContainerColor.RED
            case 5:
                return ContainerColor.PINK
            case 6:
                return ContainerColor.PURPLE
            case 7:
                return ContainerColor.VIOLET
            case 8:
                return ContainerColor.BLUE
            case 9:
                return ContainerColor.CYAN
            case 10:
                return ContainerColor.GREEN
            default:
                throw new UniFFITypeError("Unknown ContainerColor variant");
        }
    }

    static write(dataStream, value) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        if (value === ContainerColor.GRAY) {
            dataStream.writeInt32(1);
            return;
        }
        if (value === ContainerColor.YELLOW) {
            dataStream.writeInt32(2);
            return;
        }
        if (value === ContainerColor.ORANGE) {
            dataStream.writeInt32(3);
            return;
        }
        if (value === ContainerColor.RED) {
            dataStream.writeInt32(4);
            return;
        }
        if (value === ContainerColor.PINK) {
            dataStream.writeInt32(5);
            return;
        }
        if (value === ContainerColor.PURPLE) {
            dataStream.writeInt32(6);
            return;
        }
        if (value === ContainerColor.VIOLET) {
            dataStream.writeInt32(7);
            return;
        }
        if (value === ContainerColor.BLUE) {
            dataStream.writeInt32(8);
            return;
        }
        if (value === ContainerColor.CYAN) {
            dataStream.writeInt32(9);
            return;
        }
        if (value === ContainerColor.GREEN) {
            dataStream.writeInt32(10);
            return;
        }
        throw new UniFFITypeError("Unknown ContainerColor variant");
    }

    static computeSize(value) {
        return 4;
    }

    static checkType(value) {
      // Check that the value is a valid enum variant
      if (!this.#validValues.includes(value)) {
          throw new UniFFITypeError(`${value} is not a valid value for ContainerColor`);
      }
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalTypeContainerColor extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterTypeContainerColor.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterTypeContainerColor.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterTypeContainerColor.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterTypeContainerColor.computeSize(value)
    }
}



/**
 * How a container gets its label.
 */
export class ContainerLabel {}
/**
 * Name
 */
ContainerLabel.Name = class extends ContainerLabel{
   constructor({name = undefined } = {}) {
                super();
            try {
                FfiConverterString.checkType(name);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart("name");
                }
                throw e;
            }
            this.name = name;
    }
}
/**
 * Personal
 */
ContainerLabel.Personal = class extends ContainerLabel{
   constructor() {
            super();
    }
}
/**
 * Work
 */
ContainerLabel.Work = class extends ContainerLabel{
   constructor() {
            super();
    }
}
/**
 * Banking
 */
ContainerLabel.Banking = class extends ContainerLabel{
   constructor() {
            super();
    }
}
/**
 * Shopping
 */
ContainerLabel.Shopping = class extends ContainerLabel{
   constructor() {
            super();
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainerLabel extends FfiConverterArrayBuffer {
    static read(dataStream) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        switch (dataStream.readInt32()) {
            case 1:
                return new ContainerLabel.Name({
                    name: FfiConverterString.read(dataStream)
                });
            case 2:
                return new ContainerLabel.Personal();
            case 3:
                return new ContainerLabel.Work();
            case 4:
                return new ContainerLabel.Banking();
            case 5:
                return new ContainerLabel.Shopping();
            default:
                throw new UniFFITypeError("Unknown ContainerLabel variant");
        }
    }

    static write(dataStream, value) {
        // Use sequential indices (1-based) for the wire format to match the Rust scaffolding
        if (value instanceof ContainerLabel.Name) {
            dataStream.writeInt32(1);
            FfiConverterString.write(dataStream, value.name);
            return;
        }
        if (value instanceof ContainerLabel.Personal) {
            dataStream.writeInt32(2);
            return;
        }
        if (value instanceof ContainerLabel.Work) {
            dataStream.writeInt32(3);
            return;
        }
        if (value instanceof ContainerLabel.Banking) {
            dataStream.writeInt32(4);
            return;
        }
        if (value instanceof ContainerLabel.Shopping) {
            dataStream.writeInt32(5);
            return;
        }
        throw new UniFFITypeError("Unknown ContainerLabel variant");
    }

    static computeSize(value) {
        // Size of the Int indicating the variant
        let totalSize = 4;
        if (value instanceof ContainerLabel.Name) {
            totalSize += FfiConverterString.computeSize(value.name);
            return totalSize;
        }
        if (value instanceof ContainerLabel.Personal) {
            return totalSize;
        }
        if (value instanceof ContainerLabel.Work) {
            return totalSize;
        }
        if (value instanceof ContainerLabel.Banking) {
            return totalSize;
        }
        if (value instanceof ContainerLabel.Shopping) {
            return totalSize;
        }
        throw new UniFFITypeError("Unknown ContainerLabel variant");
    }

    static checkType(value) {
      if (!(value instanceof ContainerLabel)) {
        throw new UniFFITypeError(`${value} is not a subclass instance of ContainerLabel`);
      }
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalString extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterString.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterString.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterString.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterString.computeSize(value)
    }
}
/**
 * A container as the embedder sees it.
 */
export class Container {
    constructor(
        {
            userContextId, 
            isPublic, 
            icon, 
            color, 
            label, 
            policyId
        } = {
            userContextId: undefined, 
            isPublic: undefined, 
            icon: undefined, 
            color: undefined, 
            label: undefined, 
            policyId: undefined
        }
    ) {
        try {
            FfiConverterUInt32.checkType(userContextId)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("userContextId");
            }
            throw e;
        }
        try {
            FfiConverterBoolean.checkType(isPublic)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("isPublic");
            }
            throw e;
        }
        try {
            FfiConverterOptionalTypeContainerIcon.checkType(icon)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("icon");
            }
            throw e;
        }
        try {
            FfiConverterOptionalTypeContainerColor.checkType(color)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("color");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerLabel.checkType(label)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("label");
            }
            throw e;
        }
        try {
            FfiConverterOptionalString.checkType(policyId)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("policyId");
            }
            throw e;
        }
        /**
         * @type {number}
         */
        this.userContextId = userContextId;
        /**
         * @type {boolean}
         */
        this.isPublic = isPublic;
        /**
         * @type {?ContainerIcon[keyof ContainerIcon]}
         */
        this.icon = icon;
        /**
         * @type {?ContainerColor[keyof ContainerColor]}
         */
        this.color = color;
        /**
         * @type {ContainerLabel[keyof ContainerLabel]}
         */
        this.label = label;
        /**
         * @type {?string}
         */
        this.policyId = policyId;
    }

    equals(other) {
        return (
            this.userContextId == other.userContextId
            && this.isPublic == other.isPublic
            && this.icon == other.icon
            && this.color == other.color
            && this.label == other.label
            && this.policyId == other.policyId
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainer extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new Container({
            userContextId: FfiConverterUInt32.read(dataStream),
            isPublic: FfiConverterBoolean.read(dataStream),
            icon: FfiConverterOptionalTypeContainerIcon.read(dataStream),
            color: FfiConverterOptionalTypeContainerColor.read(dataStream),
            label: FfiConverterTypeContainerLabel.read(dataStream),
            policyId: FfiConverterOptionalString.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterUInt32.write(dataStream, value.userContextId);
        FfiConverterBoolean.write(dataStream, value.isPublic);
        FfiConverterOptionalTypeContainerIcon.write(dataStream, value.icon);
        FfiConverterOptionalTypeContainerColor.write(dataStream, value.color);
        FfiConverterTypeContainerLabel.write(dataStream, value.label);
        FfiConverterOptionalString.write(dataStream, value.policyId);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterUInt32.computeSize(value.userContextId);
        totalSize += FfiConverterBoolean.computeSize(value.isPublic);
        totalSize += FfiConverterOptionalTypeContainerIcon.computeSize(value.icon);
        totalSize += FfiConverterOptionalTypeContainerColor.computeSize(value.color);
        totalSize += FfiConverterTypeContainerLabel.computeSize(value.label);
        totalSize += FfiConverterOptionalString.computeSize(value.policyId);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof Container)) {
            throw new UniFFITypeError(`Expected 'Container', found '${typeof value}'`);
        }
        try {
            FfiConverterUInt32.checkType(value.userContextId);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".userContextId");
            }
            throw e;
        }
        try {
            FfiConverterBoolean.checkType(value.isPublic);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".isPublic");
            }
            throw e;
        }
        try {
            FfiConverterOptionalTypeContainerIcon.checkType(value.icon);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".icon");
            }
            throw e;
        }
        try {
            FfiConverterOptionalTypeContainerColor.checkType(value.color);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".color");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerLabel.checkType(value.label);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".label");
            }
            throw e;
        }
        try {
            FfiConverterOptionalString.checkType(value.policyId);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".policyId");
            }
            throw e;
        }
    }
}
/**
 * One of the default containers the store was opened with, shipped or supplied
 * by the embedder. Enterprise policy can replace the shipped set, and names its
 * containers rather than localizing them, so its entries carry a
 * [`ContainerLabel::Name`].
 * 
 * They seed a fresh store, and they label the default containers the user has
 * not renamed: that label is never stored, so that renaming the string behind
 * it does not need a migration.
 */
export class DefaultIdentity {
    constructor(
        {
            icon, 
            color, 
            label
        } = {
            icon: undefined, 
            color: undefined, 
            label: undefined
        }
    ) {
        try {
            FfiConverterTypeContainerIcon.checkType(icon)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("icon");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerColor.checkType(color)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("color");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerLabel.checkType(label)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("label");
            }
            throw e;
        }
        /**
         * @type {ContainerIcon[keyof ContainerIcon]}
         */
        this.icon = icon;
        /**
         * @type {ContainerColor[keyof ContainerColor]}
         */
        this.color = color;
        /**
         * @type {ContainerLabel[keyof ContainerLabel]}
         */
        this.label = label;
    }

    equals(other) {
        return (
            this.icon == other.icon
            && this.color == other.color
            && this.label == other.label
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeDefaultIdentity extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new DefaultIdentity({
            icon: FfiConverterTypeContainerIcon.read(dataStream),
            color: FfiConverterTypeContainerColor.read(dataStream),
            label: FfiConverterTypeContainerLabel.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterTypeContainerIcon.write(dataStream, value.icon);
        FfiConverterTypeContainerColor.write(dataStream, value.color);
        FfiConverterTypeContainerLabel.write(dataStream, value.label);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterTypeContainerIcon.computeSize(value.icon);
        totalSize += FfiConverterTypeContainerColor.computeSize(value.color);
        totalSize += FfiConverterTypeContainerLabel.computeSize(value.label);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof DefaultIdentity)) {
            throw new UniFFITypeError(`Expected 'DefaultIdentity', found '${typeof value}'`);
        }
        try {
            FfiConverterTypeContainerIcon.checkType(value.icon);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".icon");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerColor.checkType(value.color);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".color");
            }
            throw e;
        }
        try {
            FfiConverterTypeContainerLabel.checkType(value.label);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".label");
            }
            throw e;
        }
    }
}
/**
 * SiteAssociation
 */
export class SiteAssociation {
    constructor(
        {
            site, 
            userContextId
        } = {
            site: undefined, 
            userContextId: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(site)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("site");
            }
            throw e;
        }
        try {
            FfiConverterUInt32.checkType(userContextId)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("userContextId");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.site = site;
        /**
         * @type {number}
         */
        this.userContextId = userContextId;
    }

    equals(other) {
        return (
            this.site == other.site
            && this.userContextId == other.userContextId
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeSiteAssociation extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new SiteAssociation({
            site: FfiConverterString.read(dataStream),
            userContextId: FfiConverterUInt32.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.site);
        FfiConverterUInt32.write(dataStream, value.userContextId);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.site);
        totalSize += FfiConverterUInt32.computeSize(value.userContextId);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof SiteAssociation)) {
            throw new UniFFITypeError(`Expected 'SiteAssociation', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.site);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".site");
            }
            throw e;
        }
        try {
            FfiConverterUInt32.checkType(value.userContextId);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".userContextId");
            }
            throw e;
        }
    }
}

/**
 * Why a store could not be opened.
 */
export class InitError extends Error {}


/**
 * Carried as text rather than as a `serde_json::Error`, to keep the
 * serialization library out of the public surface.
 */
export class Malformed extends InitError {

    constructor(
        reason,
        ...params
    ) {
        const message = `reason: ${ reason }`;
        super(message, ...params);
        this.reason = reason;
    }
    toString() {
        return `Malformed: ${super.toString()}`
    }
}

/**
 * UnsupportedVersion
 */
export class UnsupportedVersion extends InitError {

    constructor(
        version,
        ...params
    ) {
        const message = `version: ${ version }`;
        super(message, ...params);
        this.version = version;
    }
    toString() {
        return `UnsupportedVersion: ${super.toString()}`
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeInitError extends FfiConverterArrayBuffer {
    static read(dataStream) {
        switch (dataStream.readInt32()) {
            case 1:
                return new Malformed(
                    FfiConverterString.read(dataStream)
                    );
            case 2:
                return new UnsupportedVersion(
                    FfiConverterUInt32.read(dataStream)
                    );
            default:
                throw new UniFFITypeError("Unknown InitError variant");
        }
    }
    static computeSize(value) {
        // Size of the Int indicating the variant
        let totalSize = 4;
        if (value instanceof Malformed) {
            totalSize += FfiConverterString.computeSize(value.reason);
            return totalSize;
        }
        if (value instanceof UnsupportedVersion) {
            totalSize += FfiConverterUInt32.computeSize(value.version);
            return totalSize;
        }
        throw new UniFFITypeError("Unknown InitError variant");
    }
    static write(dataStream, value) {
        if (value instanceof Malformed) {
            dataStream.writeInt32(1);
            FfiConverterString.write(dataStream, value.reason);
            return;
        }
        if (value instanceof UnsupportedVersion) {
            dataStream.writeInt32(2);
            FfiConverterUInt32.write(dataStream, value.version);
            return;
        }
        throw new UniFFITypeError("Unknown InitError variant");
    }

    static errorClass = InitError;
}

/**
 * A mutation that could not be applied. The store is left untouched.
 */
export class StoreError extends Error {}


/**
 * EmptyName
 */
export class EmptyName extends StoreError {

    constructor(
        ...params
    ) {
        super(...params);
    }
    toString() {
        return `EmptyName: ${super.toString()}`
    }
}

/**
 * EmptyPolicyId
 */
export class EmptyPolicyId extends StoreError {

    constructor(
        ...params
    ) {
        super(...params);
    }
    toString() {
        return `EmptyPolicyId: ${super.toString()}`
    }
}

/**
 * NoSuchContainer
 */
export class NoSuchContainer extends StoreError {

    constructor(
        userContextId,
        ...params
    ) {
        const message = `userContextId: ${ userContextId }`;
        super(message, ...params);
        this.userContextId = userContextId;
    }
    toString() {
        return `NoSuchContainer: ${super.toString()}`
    }
}

/**
 * InvalidSite
 */
export class InvalidSite extends StoreError {

    constructor(
        ...params
    ) {
        super(...params);
    }
    toString() {
        return `InvalidSite: ${super.toString()}`
    }
}

/**
 * IdSpaceExhausted
 */
export class IdSpaceExhausted extends StoreError {

    constructor(
        ...params
    ) {
        super(...params);
    }
    toString() {
        return `IdSpaceExhausted: ${super.toString()}`
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeStoreError extends FfiConverterArrayBuffer {
    static read(dataStream) {
        switch (dataStream.readInt32()) {
            case 1:
                return new EmptyName(
                    );
            case 2:
                return new EmptyPolicyId(
                    );
            case 3:
                return new NoSuchContainer(
                    FfiConverterUInt32.read(dataStream)
                    );
            case 4:
                return new InvalidSite(
                    );
            case 5:
                return new IdSpaceExhausted(
                    );
            default:
                throw new UniFFITypeError("Unknown StoreError variant");
        }
    }
    static computeSize(value) {
        // Size of the Int indicating the variant
        let totalSize = 4;
        if (value instanceof EmptyName) {
            return totalSize;
        }
        if (value instanceof EmptyPolicyId) {
            return totalSize;
        }
        if (value instanceof NoSuchContainer) {
            totalSize += FfiConverterUInt32.computeSize(value.userContextId);
            return totalSize;
        }
        if (value instanceof InvalidSite) {
            return totalSize;
        }
        if (value instanceof IdSpaceExhausted) {
            return totalSize;
        }
        throw new UniFFITypeError("Unknown StoreError variant");
    }
    static write(dataStream, value) {
        if (value instanceof EmptyName) {
            dataStream.writeInt32(1);
            return;
        }
        if (value instanceof EmptyPolicyId) {
            dataStream.writeInt32(2);
            return;
        }
        if (value instanceof NoSuchContainer) {
            dataStream.writeInt32(3);
            FfiConverterUInt32.write(dataStream, value.userContextId);
            return;
        }
        if (value instanceof InvalidSite) {
            dataStream.writeInt32(4);
            return;
        }
        if (value instanceof IdSpaceExhausted) {
            dataStream.writeInt32(5);
            return;
        }
        throw new UniFFITypeError("Unknown StoreError variant");
    }

    static errorClass = StoreError;
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalUInt32 extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterUInt32.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterUInt32.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterUInt32.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterUInt32.computeSize(value)
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeSiteAssociation extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeSiteAssociation.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeSiteAssociation.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeSiteAssociation.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeSiteAssociation.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceUInt32 extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterUInt32.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterUInt32.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterUInt32.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterUInt32.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}


// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeContainer extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeContainer.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeContainer.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeContainer.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeContainer.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalTypeContainer extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterTypeContainer.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterTypeContainer.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterTypeContainer.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterTypeContainer.computeSize(value)
    }
}



/**
 * ContainersStoreInterface
 */
export class ContainersStoreInterface {
    /**
     * create
     * @param {string} name
     * @param {ContainerIcon[keyof ContainerIcon]} icon
     * @param {ContainerColor[keyof ContainerColor]} color
     * @returns {Container}
     */
    create(
        name, 
        icon, 
        color) {
      throw Error("create not implemented");
    }
    /**
     * createForPolicy
     * @param {string} policyId
     * @returns {Container}
     */
    createForPolicy(
        policyId) {
      throw Error("createForPolicy not implemented");
    }
    /**
     * The container bound to `site`, or 0 when there is none.
     * @param {string} site
     * @returns {number}
     */
    getSiteAssociation(
        site) {
      throw Error("getSiteAssociation not implemented");
    }
    /**
     * getSiteAssociations
     * @param {?number} userContextId
     * @returns {Array.<SiteAssociation>}
     */
    getSiteAssociations(
        userContextId) {
      throw Error("getSiteAssociations not implemented");
    }
    /**
     * Moves the given public containers before `position`, or to the end when
     * it is -1. Positions below -1 are rejected.
     * @param {Array.<number>} userContextIds
     * @param {number} position
     * @returns {boolean}
     */
    moveContainers(
        userContextIds, 
        position) {
      throw Error("moveContainers not implemented");
    }
    /**
     * policyIdentities
     * @returns {Array.<Container>}
     */
    policyIdentities() {
      throw Error("policyIdentities not implemented");
    }
    /**
     * policyIdentity
     * @param {string} policyId
     * @returns {?Container}
     */
    policyIdentity(
        policyId) {
      throw Error("policyIdentity not implemented");
    }
    /**
     * privateIdentity
     * @param {string} name
     * @returns {?Container}
     */
    privateIdentity(
        name) {
      throw Error("privateIdentity not implemented");
    }
    /**
     * privateUserContextIds
     * @returns {Array.<number>}
     */
    privateUserContextIds() {
      throw Error("privateUserContextIds not implemented");
    }
    /**
     * publicIdentities
     * @returns {Array.<Container>}
     */
    publicIdentities() {
      throw Error("publicIdentities not implemented");
    }
    /**
     * publicIdentityFromId
     * @param {number} userContextId
     * @returns {?Container}
     */
    publicIdentityFromId(
        userContextId) {
      throw Error("publicIdentityFromId not implemented");
    }
    /**
     * publicUserContextIds
     * @returns {Array.<number>}
     */
    publicUserContextIds() {
      throw Error("publicUserContextIds not implemented");
    }
    /**
     * Removes a public container. Clearing the data stored under its origin
     * attributes is the embedder's job.
     * @param {number} userContextId
     * @returns {?Container}
     */
    remove(
        userContextId) {
      throw Error("remove not implemented");
    }
    /**
     * removePolicyIdentity
     * @param {number} userContextId
     * @returns {?Container}
     */
    removePolicyIdentity(
        userContextId) {
      throw Error("removePolicyIdentity not implemented");
    }
    /**
     * removeSiteAssociation
     * @param {string} site
     */
    removeSiteAssociation(
        site) {
      throw Error("removeSiteAssociation not implemented");
    }
    /**
     * The document as it stands, for the embedder to write.
     * @returns {string}
     */
    serialize() {
      throw Error("serialize not implemented");
    }
    /**
     * setSiteAssociation
     * @param {string} site
     * @param {number} userContextId
     */
    setSiteAssociation(
        site, 
        userContextId) {
      throw Error("setSiteAssociation not implemented");
    }
    /**
     * Drops the callback, so that a late mutation during teardown cannot reach
     * an embedder that is already gone.
     * 
     * One way: there is no putting it back. From here on the store keeps
     * working in memory but persists nothing.
     */
    unsetCallback() {
      throw Error("unsetCallback not implemented");
    }
    /**
     * update
     * @param {number} userContextId
     * @param {string} name
     * @param {ContainerIcon[keyof ContainerIcon]} icon
     * @param {ContainerColor[keyof ContainerColor]} color
     * @returns {?Container}
     */
    update(
        userContextId, 
        name, 
        icon, 
        color) {
      throw Error("update not implemented");
    }

}

/**
 * ContainersStore
 */
export class ContainersStore extends ContainersStoreInterface {
    // Use `init` to instantiate this class.
    // DO NOT USE THIS CONSTRUCTOR DIRECTLY
    constructor(opts) {
        super();
        if (!Object.prototype.hasOwnProperty.call(opts, constructUniffiObject)) {
            throw new UniFFIError("Attempting to construct an int using the JavaScript constructor directly" +
            "Please use a UDL defined constructor, or the init function for the primary constructor")
        }
        if (!(opts[constructUniffiObject] instanceof UniFFIPointer)) {
            throw new UniFFIError("Attempting to create a UniFFI object with a pointer that is not an instance of UniFFIPointer")
        }
        this[uniffiObjectPtr] = opts[constructUniffiObject];
    }
    /**
     * Seeds the store from a stored document, or from the default identities
     * when `bytes` is `None`. If a migration ran, the document is persisted
     * before this returns.
     * 
     * `default_identities` replaces the shipped public identities, for
     * embedders that let enterprise policy define them. They are consulted
     * whether or not there is a stored document: the label of a default
     * container the user has not renamed comes from them, since it is not
     * stored.
     * @param {?string} bytes
     * @param {?Array.<DefaultIdentity>} defaultIdentities
     * @param {ContainersCallback} callback
     * @returns {ContainersStore}
     */
    static init(
        bytes, 
        defaultIdentities, 
        callback) {
       
        FfiConverterOptionalBytes.checkType(bytes);
        FfiConverterOptionalSequenceTypeDefaultIdentity.checkType(defaultIdentities);
        FfiConverterTypeContainersCallback.checkType(callback);
        const result = UniFFIScaffolding.callSync(
            123, // uniffi_fxcontainers_fn_constructor_containersstore_new
            FfiConverterOptionalBytes.lower(bytes),
            FfiConverterOptionalSequenceTypeDefaultIdentity.lower(defaultIdentities),
            FfiConverterTypeContainersCallback.lower(callback),
        )
        return handleRustResult(
            result,
            FfiConverterTypeContainersStore.lift.bind(FfiConverterTypeContainersStore),
            FfiConverterTypeInitError.lift.bind(FfiConverterTypeInitError),
        )
    }

    /**
     * create
     * @param {string} name
     * @param {ContainerIcon[keyof ContainerIcon]} icon
     * @param {ContainerColor[keyof ContainerColor]} color
     * @returns {Container}
     */
    create(
        name, 
        icon, 
        color) {
       
        FfiConverterString.checkType(name);
        FfiConverterTypeContainerIcon.checkType(icon);
        FfiConverterTypeContainerColor.checkType(color);
        const result = UniFFIScaffolding.callSync(
            124, // uniffi_fxcontainers_fn_method_containersstore_create
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(name),
            FfiConverterTypeContainerIcon.lower(icon),
            FfiConverterTypeContainerColor.lower(color),
        )
        return handleRustResult(
            result,
            FfiConverterTypeContainer.lift.bind(FfiConverterTypeContainer),
            FfiConverterTypeStoreError.lift.bind(FfiConverterTypeStoreError),
        )
    }

    /**
     * createForPolicy
     * @param {string} policyId
     * @returns {Container}
     */
    createForPolicy(
        policyId) {
       
        FfiConverterString.checkType(policyId);
        const result = UniFFIScaffolding.callSync(
            125, // uniffi_fxcontainers_fn_method_containersstore_create_for_policy
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(policyId),
        )
        return handleRustResult(
            result,
            FfiConverterTypeContainer.lift.bind(FfiConverterTypeContainer),
            FfiConverterTypeStoreError.lift.bind(FfiConverterTypeStoreError),
        )
    }

    /**
     * The container bound to `site`, or 0 when there is none.
     * @param {string} site
     * @returns {number}
     */
    getSiteAssociation(
        site) {
       
        FfiConverterString.checkType(site);
        const result = UniFFIScaffolding.callSync(
            126, // uniffi_fxcontainers_fn_method_containersstore_get_site_association
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(site),
        )
        return handleRustResult(
            result,
            FfiConverterUInt32.lift.bind(FfiConverterUInt32),
            null,
        )
    }

    /**
     * getSiteAssociations
     * @param {?number} userContextId
     * @returns {Array.<SiteAssociation>}
     */
    getSiteAssociations(
        userContextId) {
       
        FfiConverterOptionalUInt32.checkType(userContextId);
        const result = UniFFIScaffolding.callSync(
            127, // uniffi_fxcontainers_fn_method_containersstore_get_site_associations
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterOptionalUInt32.lower(userContextId),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypeSiteAssociation.lift.bind(FfiConverterSequenceTypeSiteAssociation),
            null,
        )
    }

    /**
     * Moves the given public containers before `position`, or to the end when
     * it is -1. Positions below -1 are rejected.
     * @param {Array.<number>} userContextIds
     * @param {number} position
     * @returns {boolean}
     */
    moveContainers(
        userContextIds, 
        position) {
       
        FfiConverterSequenceUInt32.checkType(userContextIds);
        FfiConverterInt64.checkType(position);
        const result = UniFFIScaffolding.callSync(
            128, // uniffi_fxcontainers_fn_method_containersstore_move_containers
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterSequenceUInt32.lower(userContextIds),
            FfiConverterInt64.lower(position),
        )
        return handleRustResult(
            result,
            FfiConverterBoolean.lift.bind(FfiConverterBoolean),
            null,
        )
    }

    /**
     * policyIdentities
     * @returns {Array.<Container>}
     */
    policyIdentities() {
       
        const result = UniFFIScaffolding.callSync(
            129, // uniffi_fxcontainers_fn_method_containersstore_policy_identities
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypeContainer.lift.bind(FfiConverterSequenceTypeContainer),
            null,
        )
    }

    /**
     * policyIdentity
     * @param {string} policyId
     * @returns {?Container}
     */
    policyIdentity(
        policyId) {
       
        FfiConverterString.checkType(policyId);
        const result = UniFFIScaffolding.callSync(
            130, // uniffi_fxcontainers_fn_method_containersstore_policy_identity
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(policyId),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            null,
        )
    }

    /**
     * privateIdentity
     * @param {string} name
     * @returns {?Container}
     */
    privateIdentity(
        name) {
       
        FfiConverterString.checkType(name);
        const result = UniFFIScaffolding.callSync(
            131, // uniffi_fxcontainers_fn_method_containersstore_private_identity
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(name),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            null,
        )
    }

    /**
     * privateUserContextIds
     * @returns {Array.<number>}
     */
    privateUserContextIds() {
       
        const result = UniFFIScaffolding.callSync(
            132, // uniffi_fxcontainers_fn_method_containersstore_private_user_context_ids
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceUInt32.lift.bind(FfiConverterSequenceUInt32),
            null,
        )
    }

    /**
     * publicIdentities
     * @returns {Array.<Container>}
     */
    publicIdentities() {
       
        const result = UniFFIScaffolding.callSync(
            133, // uniffi_fxcontainers_fn_method_containersstore_public_identities
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypeContainer.lift.bind(FfiConverterSequenceTypeContainer),
            null,
        )
    }

    /**
     * publicIdentityFromId
     * @param {number} userContextId
     * @returns {?Container}
     */
    publicIdentityFromId(
        userContextId) {
       
        FfiConverterUInt32.checkType(userContextId);
        const result = UniFFIScaffolding.callSync(
            134, // uniffi_fxcontainers_fn_method_containersstore_public_identity_from_id
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterUInt32.lower(userContextId),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            null,
        )
    }

    /**
     * publicUserContextIds
     * @returns {Array.<number>}
     */
    publicUserContextIds() {
       
        const result = UniFFIScaffolding.callSync(
            135, // uniffi_fxcontainers_fn_method_containersstore_public_user_context_ids
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceUInt32.lift.bind(FfiConverterSequenceUInt32),
            null,
        )
    }

    /**
     * Removes a public container. Clearing the data stored under its origin
     * attributes is the embedder's job.
     * @param {number} userContextId
     * @returns {?Container}
     */
    remove(
        userContextId) {
       
        FfiConverterUInt32.checkType(userContextId);
        const result = UniFFIScaffolding.callSync(
            136, // uniffi_fxcontainers_fn_method_containersstore_remove
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterUInt32.lower(userContextId),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            null,
        )
    }

    /**
     * removePolicyIdentity
     * @param {number} userContextId
     * @returns {?Container}
     */
    removePolicyIdentity(
        userContextId) {
       
        FfiConverterUInt32.checkType(userContextId);
        const result = UniFFIScaffolding.callSync(
            137, // uniffi_fxcontainers_fn_method_containersstore_remove_policy_identity
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterUInt32.lower(userContextId),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            null,
        )
    }

    /**
     * removeSiteAssociation
     * @param {string} site
     */
    removeSiteAssociation(
        site) {
       
        FfiConverterString.checkType(site);
        const result = UniFFIScaffolding.callSync(
            138, // uniffi_fxcontainers_fn_method_containersstore_remove_site_association
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(site),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            null,
        )
    }

    /**
     * The document as it stands, for the embedder to write.
     * @returns {string}
     */
    serialize() {
       
        const result = UniFFIScaffolding.callSync(
            139, // uniffi_fxcontainers_fn_method_containersstore_serialize
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterBytes.lift.bind(FfiConverterBytes),
            null,
        )
    }

    /**
     * setSiteAssociation
     * @param {string} site
     * @param {number} userContextId
     */
    setSiteAssociation(
        site, 
        userContextId) {
       
        FfiConverterString.checkType(site);
        FfiConverterUInt32.checkType(userContextId);
        const result = UniFFIScaffolding.callSync(
            140, // uniffi_fxcontainers_fn_method_containersstore_set_site_association
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterString.lower(site),
            FfiConverterUInt32.lower(userContextId),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeStoreError.lift.bind(FfiConverterTypeStoreError),
        )
    }

    /**
     * Drops the callback, so that a late mutation during teardown cannot reach
     * an embedder that is already gone.
     * 
     * One way: there is no putting it back. From here on the store keeps
     * working in memory but persists nothing.
     */
    unsetCallback() {
       
        const result = UniFFIScaffolding.callSync(
            141, // uniffi_fxcontainers_fn_method_containersstore_unset_callback
            FfiConverterTypeContainersStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            null,
        )
    }

    /**
     * update
     * @param {number} userContextId
     * @param {string} name
     * @param {ContainerIcon[keyof ContainerIcon]} icon
     * @param {ContainerColor[keyof ContainerColor]} color
     * @returns {?Container}
     */
    update(
        userContextId, 
        name, 
        icon, 
        color) {
       
        FfiConverterUInt32.checkType(userContextId);
        FfiConverterString.checkType(name);
        FfiConverterTypeContainerIcon.checkType(icon);
        FfiConverterTypeContainerColor.checkType(color);
        const result = UniFFIScaffolding.callSync(
            142, // uniffi_fxcontainers_fn_method_containersstore_update
            FfiConverterTypeContainersStore.lowerReceiver(this),
            FfiConverterUInt32.lower(userContextId),
            FfiConverterString.lower(name),
            FfiConverterTypeContainerIcon.lower(icon),
            FfiConverterTypeContainerColor.lower(color),
        )
        return handleRustResult(
            result,
            FfiConverterOptionalTypeContainer.lift.bind(FfiConverterOptionalTypeContainer),
            FfiConverterTypeStoreError.lift.bind(FfiConverterTypeStoreError),
        )
    }

}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainersStore extends FfiConverter {
    static lift(value) {
        const opts = {};
        opts[constructUniffiObject] = value;
        return new ContainersStore(opts);
    }

    static lower(value) {
        const ptr = value[uniffiObjectPtr];
        if (!(ptr instanceof UniFFIPointer)) {
            throw new UniFFITypeError("Object is not a 'ContainersStore' instance");
        }
        return ptr;
    }

    static lowerReceiver(value) {
        // This works exactly the same as lower for non-trait interfaces
        return this.lower(value);
    }

    static read(dataStream) {
        return this.lift(dataStream.readPointer(13));
    }

    static write(dataStream, value) {
        dataStream.writePointer(13, this.lower(value));
    }

    static computeSize(value) {
        return 8;
    }
}


/**
 * How the store reaches the outside world.
 * 
 * The callback may call `serialize`, but mutating the store from it deadlocks
 * on the callback lock. Mutate on a later turn.
 */
export class ContainersCallback {
    /**
     * persist
     */
    persist() {
      throw Error("persist not implemented");
    }

}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeContainersCallback extends FfiConverter {
    static lower(callbackObj) {
        if (!(callbackObj instanceof ContainersCallback)) {
            throw new UniFFITypeError("expected 'ContainersCallback' subclass");
        }
        return uniffiCallbackHandlerContainersCallback.storeCallbackObj(callbackObj)
    }

    static lift(handleId) {
        return uniffiCallbackHandlerContainersCallback.takeCallbackObj(handleId)
    }

    static read(dataStream) {
        return this.lift(dataStream.readInt64())
    }

    static write(dataStream, callbackObj) {
        dataStream.writeInt64(this.lower(callbackObj))
    }

    static computeSize(callbackObj) {
        return 8;
    }
}
const uniffiCallbackHandlerContainersCallback = new UniFFICallbackHandler(
    "ContainersCallback",
    5,
    [
        new UniFFICallbackMethodHandler(
            "persist",
            [
            ],
            (result) => undefined,
            (e) => {
              throw e;
            }
        ),
    ]
);

// Allow the shutdown-related functionality to be tested in the unit tests
UnitTestObjs.uniffiCallbackHandlerContainersCallback = uniffiCallbackHandlerContainersCallback;
// Export the FFIConverter object to make external types work.
export class FfiConverterMapStringString extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const map = new Map();
        for (let i = 0; i < len; i++) {
            const key = FfiConverterString.read(dataStream);
            const value = FfiConverterString.read(dataStream);
            map.set(key, value);
        }

        return map;
    }

     static write(dataStream, map) {
        dataStream.writeInt32(map.size);
        for (const [key, value] of map) {
            FfiConverterString.write(dataStream, key);
            FfiConverterString.write(dataStream, value);
        }
    }

    static computeSize(map) {
        // The size of the length
        let size = 4;
        for (const [key, value] of map) {
            size += FfiConverterString.computeSize(key);
            size += FfiConverterString.computeSize(value);
        }
        return size;
    }

    static checkType(map) {
        for (const [key, value] of map) {
            try {
                FfiConverterString.checkType(key);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart("(key)");
                }
                throw e;
            }

            try {
                FfiConverterString.checkType(value);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${key}]`);
                }
                throw e;
            }
        }
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeContainerColor extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeContainerColor.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeContainerColor.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeContainerColor.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeContainerColor.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeContainerIcon extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeContainerIcon.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeContainerIcon.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeContainerIcon.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeContainerIcon.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalBytes extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterBytes.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterBytes.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterBytes.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterBytes.computeSize(value)
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeDefaultIdentity extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeDefaultIdentity.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeDefaultIdentity.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeDefaultIdentity.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeDefaultIdentity.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalSequenceTypeDefaultIdentity extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterSequenceTypeDefaultIdentity.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterSequenceTypeDefaultIdentity.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterSequenceTypeDefaultIdentity.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterSequenceTypeDefaultIdentity.computeSize(value)
    }
}
